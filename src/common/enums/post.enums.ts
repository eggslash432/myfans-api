// 投稿に関連する共通Enum定義
export enum Visibility { free = 'free', plan = 'plan', paid_single = 'paid_single' }
export enum AgeRating { all = 'all', r18 = 'r18' }
export enum Status { draft = 'draft', published = 'published' }